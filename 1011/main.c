#include <stdio.h>
#include <stdint.h>
#include <string.h>
#include <inttypes.h>
#include <stdlib.h>

int predictend(int currentspeed, int currentpos) {
    for (int i = 0; i < )
}

int main(int argc, char** argv) {
    int casecnt;
    
    scanf("%d", &casecnt);
    
    for (int i = 0; i < casecnt; i++) {
        int x, y;
        scanf("%d %d", &x, &y);
        y = y - x;
        x = 0;

        int maxmovement = 1;
        int cnt = 0;
        for (;;) {

            x += movement;

        }
    }
}